{{> head}}

<div class="jumbotron">
    {{#if user}}
    <h1>
        bienvenido, {{user.username}} espero que permanezcas en la web.
    </h1>
    {{else}}
    <h1>BIENVENIDO, home de prueba</h1>

    {{/if}}
</div>
{{> footer}}